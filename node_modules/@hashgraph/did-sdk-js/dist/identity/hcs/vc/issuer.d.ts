export declare class Issuer {
    protected id: string;
    protected name: string;
    constructor(id: string);
    constructor(id: string, name: string);
    getId(): string;
    getName(): string;
    toJsonTree(): any;
    static fromJsonTree(root: any, result?: Issuer): Issuer;
    toJSON(): string;
    static fromJson(json: string): Issuer;
}
