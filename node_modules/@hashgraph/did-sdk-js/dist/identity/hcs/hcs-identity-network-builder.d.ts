import { Client, Hbar, PublicKey, TopicId } from "@hashgraph/sdk";
import { HcsIdentityNetwork } from "./hcs-identity-network";
export declare class HcsIdentityNetworkBuilder {
    private appnetName;
    private didTopicId;
    private vcTopicId;
    private network;
    private didServers;
    private publicKey;
    private maxTransactionFee;
    private didTopicMemo;
    private vcTopicMemo;
    execute(client: Client): Promise<HcsIdentityNetwork>;
    addAppnetDidServer(serverUrl: string): HcsIdentityNetworkBuilder;
    setAppnetName(appnetName: string): HcsIdentityNetworkBuilder;
    setDidTopicMemo(didTopicMemo: string): HcsIdentityNetworkBuilder;
    setVCTopicMemo(vcTopicMemo: string): HcsIdentityNetworkBuilder;
    setDidTopicId(didTopicId: TopicId): HcsIdentityNetworkBuilder;
    setVCTopicId(vcTopicId: TopicId): HcsIdentityNetworkBuilder;
    setMaxTransactionFee(maxTransactionFee: Hbar): HcsIdentityNetworkBuilder;
    setPublicKey(publicKey: PublicKey): HcsIdentityNetworkBuilder;
    setNetwork(network: string): HcsIdentityNetworkBuilder;
}
